function smth = probe_dir( lost,of,variables)

% FUNCTION HERE INSTEAD - extract startpoint and direction for
            % next slope.
            
            if step == 0
    if numel(p_val) > 1
        if p_val(end) == p_val(end-1) 
            
            dp = dp + alpha * p_val(end);
            alpha_inner = Change_dir(alpha_inner, x_values, dp);
            pm_num = 0;
            
            k_num = slope(gm_index) - hur gör jag här!? - måste ha gradienten!;
            % dp_fun = p_fun(1); % samma tecken här
            [pt_num, step, max_t_bounds] = Next_probe(p_val, p_fun, p_trial, gm_index, l, nr, RBDO_settings, dp, alpha_values, lb, dp_fun, flag_debug);
            
        end
    end
end

